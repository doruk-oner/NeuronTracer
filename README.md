# NeuronTracer


```
git clone git@github.com:doruk-oner/NeuronTracer.git
cd NeuronTracer

conda create -n neurontrac python=3.11
conda activate neurontrac
(neurontrac) conda install -c conda-forge napari pyqt
(neurontrac) conda install dask

```